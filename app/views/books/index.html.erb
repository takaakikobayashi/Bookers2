 <h1>User info</h1>

<%= attachment_image_tag @book, :image, :fill, 100, 100, format: "jpeg", class: "img-square pull-left image", fallback: "no_image.jpg" %>
<%= @book.name %>
<%= @book.introduction %>

<%=link_to "Edit", edit_user_path %>

 <h1>Books</h1>

<% if @book.errors.any? %>
 <div id="error_explanation">
    <p><%= @book.errors.count %>errors prohibited this obj from being saved:</p>
    <ul>
      <% @book.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  <% end %>

 <table>
   	<thead>
   	<tr>
   	  <th><p></p></th>
      <th><p>Title</p></th>
      <th><p>Opinion</p></th>
    </tr>
    </thead>
<% @books.each do |book| %>
    <tbody>
    <tr>
     <td><%= attachment_image_tag book.user, :image, :fill, 100, 100, format: "jpeg", class: "img-square pull-left image", fallback: "no_image.jpg" %></td>
      <td><span>

      	<%=link_to book.title, book_path(book.id)  %>
      		
      	</span></td>
      <td><span><%= book.opinion %></span></td>
    </tr>
    </tbody>
    <% end %>
    </table>