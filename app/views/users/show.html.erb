    <h1>Users</h1>
   <% @users.each do |user| %>
      <p>image</p>
      <%= attachment_image_tag @user, :profile_image, :fill, 100, 100, format: 'jpeg' %>
      <p>name</p>
      <span><%= @user.name %></span>
   <% end %>