 <h1>Users</h1>
   <% @users.each do |user| %>
      <p>image</p>
      <%= attachment_image_tag list, :image, :fill, 100, 100, format: 'jpeg' %>
      <p>name</p>
      <span><%= name.name %></span>
   <% end %>